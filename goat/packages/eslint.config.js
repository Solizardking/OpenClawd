import pluginConfig from '@openclawdsolana/config/eslint/eslint.config.plugin.js';

/**
 * ESLint config for core
 * Uses the shared plugin config from @openclawdsolana/config
 */
export default pluginConfig;
