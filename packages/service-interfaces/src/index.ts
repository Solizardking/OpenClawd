/**
 * Main entry point for @openclawdsolana/service-interfaces
 *
 * This package provides standardized service interface definitions for OpenClawd plugins.
 * Service interfaces define contracts for plugin services without implementation details.
 *
 * Base service classes (Service, ServiceType, etc.) are imported from @openclawdsolana/core.
 */

// Export all service interfaces
export * from './interfaces';
