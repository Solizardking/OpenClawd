[tool.poetry]
name = "openclawd-plugin-lulo"
version = "0.1.1"
description = "OpenClawd plugin for lulo"
authors = [
    "Andrea Villa <andreakarimodm@gmail.com>",
]
readme = "README.md"
keywords = [
    "openclawd",
    "sdk",
    "agents",
    "ai",
    "lulo",
]
homepage = "https://openclawd.dev/"
repository = "https://github.com/openclawd/openclawd"
packages = [
    { include = "openclawd_plugins/lulo" },
]

[tool.poetry.dependencies]
python = "^3.10"
openclawd = "^0.1.0"
openclawd-wallet-solana = "^0.1.1"
aiohttp = "^3.8.6"
python-dotenv = "^1.0.0"

[tool.poetry.group.test.dependencies]
pytest = "^8.3.4"
pytest-asyncio = "^0.25.0"

[tool.poetry.group.dev.dependencies]
ruff = "^0.8.6"

[tool.poetry.group.dev.dependencies.openclawd]
path = "../../openclawd"
develop = true

[tool.poetry.group.dev.dependencies.openclawd-wallet-solana]
path = "../../wallets/solana"
develop = true

[tool.poetry.urls]
"Bug Tracker" = "https://github.com/openclawd/openclawd/issues"

[tool.pytest.ini_options]
addopts = [
    "--import-mode=importlib",
]
pythonpath = "src"
asyncio_default_fixture_loop_scope = "function"

[tool.ruff]
line-length = 120
target-version = "py312"

[build-system]
requires = [
    "poetry-core",
]
build-backend = "poetry.core.masonry.api"
