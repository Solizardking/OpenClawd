[tool.poetry]
name = "openclawd-wallet-multiversx"
version = "0.1.0"
description = "OpenClawd MultiversX wallet implementation"
authors = [
    "Ofem Eteng <ofemeteng@gmail.com>",
]
readme = "README.md"
keywords = [
    "openclawd",
    "sdk",
    "multiversx",
    "agents",
    "ai",
    "wallet",
]
homepage = "https://openclawd.dev/"
repository = "https://github.com/openclawd/openclawd"
packages = [
    { include = "openclawd_wallets/multiversx" },
]

[tool.poetry.dependencies]
python = "^3.10"
openclawd = "^0.1.0"
pydantic = "^2.0.0"
multiversx-sdk = ">=1.1.1"

[tool.poetry.group.test.dependencies]
pytest = "^8.3.4"
pytest-asyncio = "^0.25.0"

[tool.poetry.group.dev.dependencies]
ruff = "^0.8.6"

[tool.poetry.group.dev.dependencies.openclawd]
path = "../../openclawd"
develop = true

[tool.poetry.urls]
"Bug Tracker" = "https://github.com/openclawd/openclawd/issues"

[tool.pytest.ini_options]
addopts = [
    "--import-mode=importlib",
]
pythonpath = "src"
asyncio_default_fixture_loop_scope = "function"

[tool.ruff]
line-length = 120
target-version = "py312"

[build-system]
requires = [
    "poetry-core",
]
build-backend = "poetry.core.masonry.api"
