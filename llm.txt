# OpenClawd · LLM Context File

## What We Just Did (2026-04-25)

### 1. Public Release Preparation

Prepared the OpenClawd monorepo for public release with the following enhancements:

#### Created `.gitignore`
Comprehensive ignore file protecting against:
- Dependencies: `node_modules/`, `.pnpm-store/`
- Environment files: `.env`, `.env.local`, `.env.*.local`
- Secrets: `*.pem`, `*.key`, `*.p8`, `*.jwk`, `secrets/`, `keys/`
- Build outputs: `dist/`, `build/`, `.output/`, `.nitro/`
- Logs: `*.log`, npm/pnpm/yarn debug logs
- IDE files: `.vscode/`, `.idea/`
- OS files: `.DS_Store`, `Thumbs.db`
- Lock files: `package-lock.json`, `pnpm-lock.yaml`, `yarn.lock`
- Cloud: `.vercel`, `.netlify`, GCP credentials
- Crypto-specific: `*.json!/keypair.json`
- Runtime caches: `.mcp/`, `.e2b/`, `.honcho/`, `session-logs/`

#### Created `scripts/` Directory with 4 Hygiene Scripts

**`scripts/install-git-hooks.mjs`**
- Installs Git hooks for security
- pre-commit hook: blocks commits with staged secrets
- pre-push hook: runs hygiene checks before pushing

**`scripts/doctor.mjs`**
- Verifies bootstrap requirements
- Checks: Node.js version (20+), package.json, README.md, LICENSE.md, .gitignore, AGENTS/, skills/, scripts/

**`scripts/guard-secrets.mjs`**
- Scans codebase for leaked credentials
- Detects: OpenAI keys, OpenRouter keys, private key blocks, Slack/GitHub/AWS tokens
- Excludes known-safe patterns:
  - `11111111111111111111111111111111` (Solana System Program address)
  - `AKIAIOSFODNN7EXAMPLE` (AWS example key)
  - `c8a8aaeb7ef3fbcce40bada2196e2bcb` (known example hash)
  - `0bc83cb571cd1c50ba6f3e8a78ef1346` (Gravatar example MD5)
- Can run on `--staged` or `--worktree` files

**`scripts/brand-check.mjs`**
- Catches old brand references in docs
- Checks for: solanaos, nanosolana, nanohub, solana-clawd, clawd3d

**`scripts/release-check.mjs`**
- Public-release hygiene verification
- Checks: package.json metadata, README content, LICENSE, .gitignore, .env protection, catalog files

### 2. Git Hooks Installed
```
✅ Installed pre-commit hook
✅ Installed pre-push hook
```

### 3. Verification Results
```
🏥 Doctor:          ✅ 8/8 checks passed
🚀 Release Check:   ✅ 9/9 checks passed  
🔒 Guard Secrets:  ✅ 0 leaks (8,841 files scanned)
```

---

## Quick Start Commands
```bash
# Install dependencies
npm run install:all

# Build agent catalog
npm run build:catalog

# Install git hooks
npm run hooks:install

# Run checks
npm run doctor
npm run release:check
npm run guard:worktree
npm run brand:check
```

---

## Project Overview

**OpenClawd** - Solana-native AI agent platform  
**$CLAWD Token**: `8cHzQHUS2s2h8TzCmfqPKYiM4dSt4roa3n7MyRLApump`  
**Site**: https://solanaclawd.com

### Key Directories
| Path | Purpose |
|------|---------|
| `agents/` | Agent catalog (50+ agents) |
| `skills/` | Skills marketplace (150+ skills) |
| `packages/` | SDKs: clawd-wallet, agents-x402-solana, percolator |
| `MCP/` | MCP servers: vault-mcp, wurk-mcp |
| `clawdhub/` | Skills marketplace & hub CLI |
| `clawd-code-cli/` | Terminal-native coding surface |
| `openclawd-framework/` | Agent framework |
| `solana-attestation-service-master/` | On-chain SAS program |

### Security Features
- ClawdVault security scanning
- Solana Attestation Service (SAS) for on-chain verification
- Hermès Vault Protocol for agent wallet protection
- x402/AP2 payment rails

---

## Previous Work (from prior session)

### 1. Enhanced install.sh with More Lobster Theming
- Elaborate ASCII lobster banner with 🦞 emoji
- "OPENCLAWD" printed in block text with multi-color lobster gradient
- Lobster-themed colors: LOBSTER1, LOBSTER2, LOBSTER3, CORAL, SALMON
- Lobster dance and wave animation frames

### 2. Cloudflare Worker Gateway
- Routes: `install.solanaclawd.com`, `gateway.solanaclawd.com`
- Cyberpunk lobster-themed HTML gateway
- Copy-to-clipboard install command
- Feature cards display

### 3. Key URLs
- Install script: `https://solanaclawd.com/install.sh`
- Gateway: `https://solanaclawd.com/gateway`
- Token: `8cHzQHUS2s2h8TzCmfqPKYiM4dSt4roa3n7MyRLApump`
- Hotline: 909-413-5567

---

## Deploy Commands
```bash
# Deploy Cloudflare Worker
cd workers/install-worker && npm install && npx wrangler deploy

# Sync install.sh to worker
cd workers/install-worker && npm run sync
```
